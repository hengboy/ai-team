# 任务拆分

<!--
填写规则：
1. 每个任务标题必须使用“### [ ] TASK-xxx：标题”；只有全部验收标准通过且证据齐全后，才能改为“### [x] TASK-xxx：标题”。
2. 任务按依赖顺序编号；依赖未完成或出现阻塞时，不得开始后续任务。
3. 每个任务只能写入“允许写入路径”，不得扩大范围或顺手修改无关文件。
4. 依赖关系、并行批次和各任务的前置任务、写入范围必须一致。
-->

## 任务清单

### [ ] TASK-001：<任务标题>

- 目标：
- 需求覆盖：REQ-001
- 验收覆盖：AC-001
- 前置任务：无
- 输入与已确认依据：
- 读取范围：
- 写入范围：
- 允许写入路径：
- 禁止写入路径：
- 实施步骤（按顺序）：
  1.
- 完成条件：
  - 覆盖的 REQ/AC 均已实现。
  - 自测命令全部通过并记录证据。
  - 交接内容完整，且没有未解决阻塞。
- 验收证据：
  - AC-001：命令或证据路径；预期结果。
- 自测命令：
- 失败与阻塞处理：停止任务，不勾选复选框，保留现场并请求所需支持。
- 交接内容：变更摘要、文件路径、验证结果、剩余风险和下一任务所需输入。

## 依赖关系

```text
TASK-001
```

- 箭头表示“前者完成后才能开始后者”；无依赖任务单独列出。

## 并行批次

| 批次 | 前置批次 | 任务 | 不重叠写入范围 | 开始条件 |
| --- | --- | --- | --- | --- |
| 1 | 无 | TASK-001 | `src/example/**` | 输入已确认且写入范围无重叠 |

- 同一批次只允许放置无依赖且写入范围不重叠的任务；任一条件不满足时必须拆到后续批次。

## 风险与阻塞

- 风险：影响：触发信号：缓解措施：
- 阻塞：受影响任务：需要的决策或支持：恢复条件：
